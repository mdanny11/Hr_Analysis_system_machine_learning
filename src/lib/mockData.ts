// Types only — data is fetched from the API (see src/services/api.ts)
export type {
  Employee,
  Department,
  PredictionModel,
  AuditLog,
  KpiMetrics,
  AttritionTrend,
  FeatureImportance,
  EmployeeStats,
} from '@/lib/types';
